version: 2

sphinx:
  configuration: docs/conf.py
  builder: html

build:
  apt_packages:
    - cmake
    - g++

python:
  version: "3.8"
  install:
    - requirements: docs/requirements.txt
      extra_requirements:
        - chrysalis-st
  system_packages: true
